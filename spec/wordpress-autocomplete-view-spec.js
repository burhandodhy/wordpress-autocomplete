'use babel';

import WordpressAutocompleteView from '../lib/wordpress-autocomplete-view';

describe('WordpressAutocompleteView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
